# SPDX-FileCopyrightText: 2026 Institute for Automation of Complex Power Systems, EONERC, RWTH Aachen University
# SPDX-License-Identifier: MPL-2.0

name: Build CI

on:
  workflow_call:
    inputs:
      version:
        description: "Release version tag (e.g. v1.2.3 or latest). If set, triggers release tagging."
        required: false
        type: string
        default: ''

      base_tag:
        description: Image tag to propagate context to and from other workflows
        required: false
        type: string
        default: ${{ github.sha }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ORGANIZATION: sogno-platform
  REGISTRIES: '["docker.io"]'
jobs:
  setup:
    name: Resolve images

    runs-on: ubuntu-24.04

    outputs:
      image_repos: ${{ steps.images.outputs.image_repos }}
      registries: ${{ steps.config.outputs.registries }}
      version: ${{ steps.config.outputs.version }}
      base_tag: ${{ steps.config.outputs.base_tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.base_tag }}

      - name: Resolve configuration
        id: config
        shell: bash
        env:
          INPUT_REGISTRIES: ${{ env.REGISTRIES }}
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_BASE_TAG: ${{ inputs.base_tag }}
        run: |
          set -euo pipefail

          # ---- validate registries as array of string ----
          echo "${INPUT_REGISTRIES}" | jq -e '
            type == "array"
            and length > 0
            and all(.[]; type == "string")
          ' >/dev/null

          echo "registries=${INPUT_REGISTRIES}" >> "${GITHUB_OUTPUT}"
          echo "version=${INPUT_VERSION}" >> "${GITHUB_OUTPUT}"
          echo "base_tag=${INPUT_BASE_TAG}" >> "${GITHUB_OUTPUT}"

      - name: Resolve images
        id: images
        shell: bash
        run: |
          set -euo pipefail

          image_repos="$(
            find packaging/Docker \
              -maxdepth 1 \
              -type f \
              -name 'Dockerfile.*' \
              -printf '%f\n' |
            sed 's/^Dockerfile\.//' |
            sort -u |
            jq -R -s -c '
              split("\n")
              | map(select(length > 0))
              | map(select(. != "dev"))
              | map({
                  platform: .,
                  repo: (if . == "dpsim" then "dpsim" else "dpsim-" + . end)
                })
            '
          )"

          echo "Image Repositories: ${image_repos}"
          echo "image_repos=${image_repos}" >> "${GITHUB_OUTPUT}"

  build-dev:
    name: Build & Cache dpsim-dev
    needs: setup
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.base_tag }}

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Base Image to Staging Registry
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packaging/Docker/Dockerfile.dev
          tags: ghcr.io/${{ env.ORGANIZATION }}/staging/dpsim-dev:${{ needs.setup.outputs.base_tag }}
          push: true

  build:
    name: Build ${{ matrix.target.repo }}
    needs:
      - setup
      - build-dev
    if: |
      !cancelled() &&
      needs.setup.outputs.image_repos != '[]' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.setup.outputs.image_repos) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.setup.outputs.base_tag }}

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build images
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packaging/Docker/Dockerfile.${{ matrix.target.platform }}
          build-args: |
            BASE_IMAGE=ghcr.io/${{ env.ORGANIZATION }}/staging/dpsim-dev:${{ needs.setup.outputs.base_tag }}
          tags: |
            ghcr.io/${{ env.ORGANIZATION }}/staging/${{ matrix.target.repo }}:${{ needs.setup.outputs.base_tag }}
          push: true

  push-dev:
    name: Promote and push dev image
    needs:
      - setup
      - build-dev
      - build
    if: |
      !cancelled() &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-24.04
    steps:
      - name: Log in to Docker Hub
        if: contains(fromJSON(needs.setup.outputs.registries), 'docker.io')
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_SECRET }}

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve metadata (docker.io)
        id: meta-dockerhub
        if: contains(fromJSON(needs.setup.outputs.registries), 'docker.io')
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ env.ORGANIZATION }}/dpsim-dev
          tags: |
            type=raw,value=${{ needs.setup.outputs.base_tag }}
            type=raw,value=${{ needs.setup.outputs.version }},enable=${{ needs.setup.outputs.version != '' }}
            type=raw,value=latest,enable=${{ needs.setup.outputs.version != '' }}

      - name: Resolve legacy metadata (docker.io)
        id: meta-dockerhub-legacy
        if: contains(fromJSON(needs.setup.outputs.registries), 'docker.io') && needs.setup.outputs.version != ''
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ env.ORGANIZATION }}/dpsim
          tags: |
            type=raw,value=dev

      - name: Resolve metadata (ghcr.io)
        id: meta-ghcr
        if: contains(fromJSON(needs.setup.outputs.registries), 'ghcr.io')
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.ORGANIZATION }}/dpsim-dev
          tags: |
            type=raw,value=${{ needs.setup.outputs.base_tag }}
            type=raw,value=${{ needs.setup.outputs.version }},enable=${{ needs.setup.outputs.version != '' }}
            type=raw,value=latest,enable=${{ needs.setup.outputs.version != '' }}

      - name: Resolve legacy metadata (ghcr.io)
        id: meta-ghcr-legacy
        if: contains(fromJSON(needs.setup.outputs.registries), 'ghcr.io') && needs.setup.outputs.version != ''
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.ORGANIZATION }}/dpsim
          tags: |
            type=raw,value=dev

      - name: Pull from Staging and Push to Target Registries
        shell: bash
        env:
          BASE_TAG: ${{ needs.setup.outputs.base_tag }}
          TAGS: |
            ${{ steps.meta-dockerhub.outputs.tags }}
            ${{ steps.meta-dockerhub-legacy.outputs.tags }}
            ${{ steps.meta-ghcr.outputs.tags }}
            ${{ steps.meta-ghcr-legacy.outputs.tags }}
        run: |
          set -euo pipefail
          staging_image="ghcr.io/${ORGANIZATION}/staging/dpsim-dev:${BASE_TAG}"
          docker pull "${staging_image}"

          while IFS= read -r tag; do
            [[ -z "${tag}" ]] && continue
            docker tag "${staging_image}" "${tag}"
            docker push "${tag}"
          done <<< "${TAGS}"

  push:
    name: Promote and push ${{ matrix.target.repo }}
    needs:
      - setup
      - build
    if: |
      !cancelled() &&
      needs.setup.outputs.image_repos != '[]' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJSON(needs.setup.outputs.image_repos) }}
    steps:
      - name: Log in to Docker Hub
        if: contains(fromJSON(needs.setup.outputs.registries), 'docker.io')
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_SECRET }}

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve metadata (docker.io)
        id: meta-dockerhub
        if: contains(fromJSON(needs.setup.outputs.registries), 'docker.io')
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ env.ORGANIZATION }}/${{ matrix.target.repo }}
          tags: |
            type=raw,value=${{ needs.setup.outputs.base_tag }}
            type=raw,value=${{ needs.setup.outputs.version }},enable=${{ needs.setup.outputs.version != '' }}
            type=raw,value=latest,enable=${{ needs.setup.outputs.version != '' }}

      - name: Resolve legacy transition metadata (docker.io)
        id: meta-dockerhub-legacy
        if: |
          contains(fromJSON(needs.setup.outputs.registries), 'docker.io') && 
          needs.setup.outputs.version != '' && 
          matrix.target.repo != 'dpsim'
        uses: docker/metadata-action@v5
        with:
          images: docker.io/${{ env.ORGANIZATION }}/dpsim
          tags: |
            type=raw,value=${{ matrix.target.platform }}

      - name: Resolve metadata (ghcr.io)
        id: meta-ghcr
        if: contains(fromJSON(needs.setup.outputs.registries), 'ghcr.io')
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.ORGANIZATION }}/${{ matrix.target.repo }}
          tags: |
            type=raw,value=${{ needs.setup.outputs.base_tag }}
            type=raw,value=${{ needs.setup.outputs.version }},enable=${{ needs.setup.outputs.version != '' }}
            type=raw,value=latest,enable=${{ needs.setup.outputs.version != '' }}

      - name: Resolve legacy transition metadata (ghcr.io)
        id: meta-ghcr-legacy
        if: |
          contains(fromJSON(needs.setup.outputs.registries), 'ghcr.io') && 
          needs.setup.outputs.version != '' && 
          matrix.target.repo != 'dpsim'
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ env.ORGANIZATION }}/dpsim
          tags: |
            type=raw,value=${{ matrix.target.platform }}

      - name: Pull from Staging and Push to Target Registries
        shell: bash
        env:
          BASE_TAG: ${{ needs.setup.outputs.base_tag }}
          IMAGE_REPO: ${{ matrix.target.repo }}
          TAGS: |
            ${{ steps.meta-dockerhub.outputs.tags }}
            ${{ steps.meta-dockerhub-legacy.outputs.tags }}
            ${{ steps.meta-ghcr.outputs.tags }}
            ${{ steps.meta-ghcr-legacy.outputs.tags }}
        run: |
          set -euo pipefail
          staging_image="ghcr.io/${ORGANIZATION}/staging/${IMAGE_REPO}:${BASE_TAG}"
          docker pull "${staging_image}"

          while IFS= read -r tag; do
            [[ -z "${tag}" ]] && continue
            docker tag "${staging_image}" "${tag}"
            docker push "${tag}"
          done <<< "${TAGS}"